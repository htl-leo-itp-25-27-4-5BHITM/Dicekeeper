apiVersion: apps/v1
kind: Deployment
metadata:
  name: keycloak
  namespace: student-it200233
  labels:
    app: keycloak
spec:
  replicas: 1
  selector:
    matchLabels:
      app: keycloak
  template:
    metadata:
      labels:
        app: keycloak
    spec:
      containers:
        - name: keycloak
          image: ghcr.io/htl-leo-itp-25-27-4-5bhitm/keycloak-dicekeeper:latest
          imagePullPolicy: Always
          args:
            - start
          env:
            - name: KC_HTTP_ENABLED
              value: "true"
            - name: KC_PROXY_HEADERS
              value: xforwarded
            - name: KC_HOSTNAME
              value: https://auth.dicekeeper.net
          envFrom:
            - secretRef:
                name: keycloak-secret
          ports:
            - containerPort: 8080
      imagePullSecrets:
        - name: ghcr-pull-secret
